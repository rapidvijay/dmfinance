
@{
    ViewBag.Title = "Add Area";
    Layout = null;
}


@model PagedList.IPagedList<ModificationHousingFeesAdmin.Models.Area>
@using GridMvc.Html


<!-- Modal --> 
<div class="modal-dialog" >
    <div class="modal-content">
        <div class="modal-header">
            <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
            <h4 class="modal-title">Add Area</h4>
        </div>
        @*using (Ajax.BeginForm("Create", "PropertyTypes", null, new AjaxOptions { HttpMethod = "Post", OnSuccess = "CreateAssetSuccess" }, new { @class = "form-horizontal", role = "form" }))
            {*@
        <div class="modal-body">

            
                    <div class="row">
                        <div class="col-md-12">
                            <input type="hidden" value="@ViewBag.propertyId" id="propertyId" name="propertyId" />
                            <p id="info"> </p>
                            <table id="TableVales" class="table table-striped table-bordered" width="100%">
                                <thead>
                                    <tr>
                                        <th>Reference Num</th>
                                        <th>Arabic Name</th>
                                        <th>English Name</th>
                                        <th>Action</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @if (Model != null)
                                    {
                                        foreach (var item in Model)
                                        {
                                            <tr>
                                                <td> @item.Id</td>
                                                <td> @item.Arabic_Name</td>
                                                <td> @item.English_Name</td>
                                                <td><a href="#"><span class="fa fa-check" onclick="return saveNewArea(@item.Id)"></span></a></td>
                                            </tr>
                                        }
                                    }
                                </tbody>
                            </table>
                            @*@if (Model != null)
                                 {
                                     @Html.Grid(Model).Named("HousingFeesGrid").Columns(columns =>
                                {
                                    columns.Add(c => c.Id).Titled("Reference Num").Filterable(true);
                                    columns.Add(c => c.Arabic_Name).Titled("Arabic Name").Filterable(true);
                                    columns.Add(c => c.English_Name).Titled("English Name").Filterable(true);

                                    columns.Add()
                                    .Encoded(false)
                                    .Sanitized(false)
                                    .SetWidth(30)
                                    .RenderValueAs(o => Html.ActionLink("Select", "#", "", null, new { @class = "fa  btn-details", onclick = "return saveNewArea(" + o.Id + ");" }));
                                }).WithPaging(4).Filterable(true).Sortable(true)
                                 }*@


                        </div>
                    </div>
               

        </div>
        <div class="modal-footer">
            <div class="form-group">
                <div class="col-md-offset-2 col-md-10">
                    <button type="button" class="btn btn-info" data-dismiss="modal">Close</button>
                    @*<input type="submit" class="btn btn-primary" value="Save" />*@
                </div>
            </div>
        </div>
        @*}*@
    </div>
</div>
<script>
    $(document).ready(function () { 
        $('#TableVales').DataTable({
            "pageLength": 3
        });
    });
</script>   